accessible @ https://jstefek.github.io/events-calendar/

To build the project just call `webpack` from project root directory (need to have webpack installed with npm).
To run the project just open the `index.html`.